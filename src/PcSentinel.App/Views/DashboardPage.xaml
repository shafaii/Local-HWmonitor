<Page
    x:Class="PcSentinel.App.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:PcSentinel.App.ViewModels"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="20" Margin="0,0,16,24">

            <!-- Top Header & Controls -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Spacing="4">
                    <TextBlock Text="System Dashboard" FontSize="26" FontWeight="SemiBold" Foreground="#FFFFFF" />
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="{x:Bind ViewModel.LastUpdatedText, Mode=OneWay}" FontSize="13" Foreground="#9E9E9E" />
                        <TextBlock Text="•" FontSize="13" Foreground="#616161" />
                        <TextBlock Text="{x:Bind ViewModel.SystemHealthStatus, Mode=OneWay}" FontSize="13" FontWeight="Medium" Foreground="#4CAF50" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                    <Button Command="{x:Bind ViewModel.RefreshNowCommand}">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <FontIcon Glyph="&#xE72C;" FontSize="12" />
                            <TextBlock Text="Refresh" FontSize="12" />
                        </StackPanel>
                    </Button>
                    <Button Command="{x:Bind ViewModel.ToggleMonitoringCommand}" Style="{ThemeResource AccentButtonStyle}">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <FontIcon Glyph="&#xE768;" FontSize="12" />
                            <TextBlock Text="Pause Stream" FontSize="12" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- 4 Summary Cards Grid (CPU, GPU, Memory, Storage) -->
            <Grid ColumnSpacing="16" RowSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- CPU Card -->
                <Border Grid.Column="0" Background="#202020" CornerRadius="8" Padding="16" BorderBrush="#333333" BorderThickness="1">
                    <StackPanel Spacing="12">
                        <Grid>
                            <TextBlock Text="PROCESSOR (CPU)" FontSize="11" FontWeight="Bold" Foreground="#8A8886" />
                            <TextBlock Text="{x:Bind ViewModel.CpuCard.StatusTag, Mode=OneWay}" HorizontalAlignment="Right" FontSize="11" Foreground="#107C41" FontWeight="SemiBold" />
                        </Grid>
                        <TextBlock Text="{x:Bind ViewModel.CpuCard.Subtitle, Mode=OneWay}" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" Foreground="#FFFFFF" />
                        
                        <Grid>
                            <TextBlock Text="{x:Bind ViewModel.CpuCard.PrimaryPercentageFormatted, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="#0078D4" />
                            <TextBlock Text="TOTAL LOAD" FontSize="10" Foreground="#8A8886" HorizontalAlignment="Right" VerticalAlignment="Bottom" />
                        </Grid>
                        <ProgressBar Value="{x:Bind ViewModel.CpuCard.PrimaryPercentage, Mode=OneWay}" Maximum="100" Height="4" Foreground="#0078D4" Background="#333333" />

                        <!-- Live CPU Sparkline -->
                        <Border Background="#181818" CornerRadius="4" Padding="4" Height="40">
                            <Grid>
                                <Polyline Points="{x:Bind ViewModel.CpuCard.SparklinePoints, Mode=OneWay}" Stroke="#0078D4" StrokeThickness="1.5" />
                                <TextBlock Text="{x:Bind ViewModel.CpuCard.SparklineMinMaxText, Mode=OneWay}" FontSize="9" Foreground="#666666" HorizontalAlignment="Right" VerticalAlignment="Top" />
                            </Grid>
                        </Border>

                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Temp" FontSize="10" Foreground="#8A8886" />
                                <TextBlock Text="{x:Bind ViewModel.CpuCard.TemperatureText, Mode=OneWay}" FontSize="13" FontWeight="SemiBold" Foreground="#E0E0E0" />
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Package Power" FontSize="10" Foreground="#8A8886" />
                                <TextBlock Text="{x:Bind ViewModel.CpuCard.PowerOrCapacityText, Mode=OneWay}" FontSize="13" FontWeight="SemiBold" Foreground="#E0E0E0" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- GPU Card -->
                <Border Grid.Column="1" Background="#202020" CornerRadius="8" Padding="16" BorderBrush="#333333" BorderThickness="1">
                    <StackPanel Spacing="12">
                        <Grid>
                            <TextBlock Text="GRAPHICS (GPU)" FontSize="11" FontWeight="Bold" Foreground="#8A8886" />
                            <TextBlock Text="{x:Bind ViewModel.GpuCard.StatusTag, Mode=OneWay}" HorizontalAlignment="Right" FontSize="11" Foreground="#107C41" FontWeight="SemiBold" />
                        </Grid>
                        <TextBlock Text="{x:Bind ViewModel.GpuCard.Subtitle, Mode=OneWay}" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" Foreground="#FFFFFF" />

                        <Grid>
                            <TextBlock Text="{x:Bind ViewModel.GpuCard.PrimaryPercentageFormatted, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="#107C41" />
                            <TextBlock Text="CORE LOAD" FontSize="10" Foreground="#8A8886" HorizontalAlignment="Right" VerticalAlignment="Bottom" />
                        </Grid>
                        <ProgressBar Value="{x:Bind ViewModel.GpuCard.PrimaryPercentage, Mode=OneWay}" Maximum="100" Height="4" Foreground="#107C41" Background="#333333" />

                        <!-- Live GPU Sparkline -->
                        <Border Background="#181818" CornerRadius="4" Padding="4" Height="40">
                            <Grid>
                                <Polyline Points="{x:Bind ViewModel.GpuCard.SparklinePoints, Mode=OneWay}" Stroke="#107C41" StrokeThickness="1.5" />
                                <TextBlock Text="{x:Bind ViewModel.GpuCard.SparklineMinMaxText, Mode=OneWay}" FontSize="9" Foreground="#666666" HorizontalAlignment="Right" VerticalAlignment="Top" />
                            </Grid>
                        </Border>

                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Core Temp" FontSize="10" Foreground="#8A8886" />
                                <TextBlock Text="{x:Bind ViewModel.GpuCard.TemperatureText, Mode=OneWay}" FontSize="13" FontWeight="SemiBold" Foreground="#E0E0E0" />
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Fan Speed" FontSize="10" Foreground="#8A8886" />
                                <TextBlock Text="{x:Bind ViewModel.GpuCard.PowerOrCapacityText, Mode=OneWay}" FontSize="13" FontWeight="SemiBold" Foreground="#E0E0E0" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Memory Card -->
                <Border Grid.Column="2" Background="#202020" CornerRadius="8" Padding="16" BorderBrush="#333333" BorderThickness="1">
                    <StackPanel Spacing="12">
                        <Grid>
                            <TextBlock Text="PHYSICAL MEMORY (RAM)" FontSize="11" FontWeight="Bold" Foreground="#8A8886" />
                            <TextBlock Text="{x:Bind ViewModel.MemoryCard.StatusTag, Mode=OneWay}" HorizontalAlignment="Right" FontSize="11" Foreground="#107C41" FontWeight="SemiBold" />
                        </Grid>
                        <TextBlock Text="{x:Bind ViewModel.MemoryCard.Subtitle, Mode=OneWay}" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" Foreground="#FFFFFF" />

                        <Grid>
                            <TextBlock Text="{x:Bind ViewModel.MemoryCard.PrimaryPercentageFormatted, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="#FFB900" />
                            <TextBlock Text="UTILIZATION" FontSize="10" Foreground="#8A8886" HorizontalAlignment="Right" VerticalAlignment="Bottom" />
                        </Grid>
                        <ProgressBar Value="{x:Bind ViewModel.MemoryCard.PrimaryPercentage, Mode=OneWay}" Maximum="100" Height="4" Foreground="#FFB900" Background="#333333" />

                        <!-- Live Memory Sparkline -->
                        <Border Background="#181818" CornerRadius="4" Padding="4" Height="40">
                            <Grid>
                                <Polyline Points="{x:Bind ViewModel.MemoryCard.SparklinePoints, Mode=OneWay}" Stroke="#FFB900" StrokeThickness="1.5" />
                                <TextBlock Text="{x:Bind ViewModel.MemoryCard.SparklineMinMaxText, Mode=OneWay}" FontSize="9" Foreground="#666666" HorizontalAlignment="Right" VerticalAlignment="Top" />
                            </Grid>
                        </Border>

                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Used Memory" FontSize="10" Foreground="#8A8886" />
                                <TextBlock Text="{x:Bind ViewModel.MemoryCard.PowerOrCapacityText, Mode=OneWay}" FontSize="13" FontWeight="SemiBold" Foreground="#E0E0E0" />
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Available" FontSize="10" Foreground="#8A8886" />
                                <TextBlock Text="{x:Bind ViewModel.MemoryCard.TemperatureText, Mode=OneWay}" FontSize="13" FontWeight="SemiBold" Foreground="#E0E0E0" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Storage Card -->
                <Border Grid.Column="3" Background="#202020" CornerRadius="8" Padding="16" BorderBrush="#333333" BorderThickness="1">
                    <StackPanel Spacing="12">
                        <Grid>
                            <TextBlock Text="PRIMARY STORAGE" FontSize="11" FontWeight="Bold" Foreground="#8A8886" />
                            <TextBlock Text="{x:Bind ViewModel.StorageCard.StatusTag, Mode=OneWay}" HorizontalAlignment="Right" FontSize="11" Foreground="#107C41" FontWeight="SemiBold" />
                        </Grid>
                        <TextBlock Text="{x:Bind ViewModel.StorageCard.Subtitle, Mode=OneWay}" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" Foreground="#FFFFFF" />

                        <Grid>
                            <TextBlock Text="{x:Bind ViewModel.StorageCard.PrimaryPercentageFormatted, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="#8764B8" />
                            <TextBlock Text="SPACE USED" FontSize="10" Foreground="#8A8886" HorizontalAlignment="Right" VerticalAlignment="Bottom" />
                        </Grid>
                        <ProgressBar Value="{x:Bind ViewModel.StorageCard.PrimaryPercentage, Mode=OneWay}" Maximum="100" Height="4" Foreground="#8764B8" Background="#333333" />

                        <!-- Live Storage Sparkline -->
                        <Border Background="#181818" CornerRadius="4" Padding="4" Height="40">
                            <Grid>
                                <Polyline Points="{x:Bind ViewModel.StorageCard.SparklinePoints, Mode=OneWay}" Stroke="#8764B8" StrokeThickness="1.5" />
                                <TextBlock Text="{x:Bind ViewModel.StorageCard.SparklineMinMaxText, Mode=OneWay}" FontSize="9" Foreground="#666666" HorizontalAlignment="Right" VerticalAlignment="Top" />
                            </Grid>
                        </Border>

                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Drive Temp" FontSize="10" Foreground="#8A8886" />
                                <TextBlock Text="{x:Bind ViewModel.StorageCard.TemperatureText, Mode=OneWay}" FontSize="13" FontWeight="SemiBold" Foreground="#E0E0E0" />
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Free Capacity" FontSize="10" Foreground="#8A8886" />
                                <TextBlock Text="{x:Bind ViewModel.StorageCard.PowerOrCapacityText, Mode=OneWay}" FontSize="13" FontWeight="SemiBold" Foreground="#E0E0E0" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Live Sensor Feed Section -->
            <Border Background="#202020" CornerRadius="8" Padding="20" BorderBrush="#333333" BorderThickness="1">
                <StackPanel Spacing="16">
                    <Grid>
                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <TextBlock Text="Live Telemetry Feed" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFFF" />
                            <Border Background="#333333" CornerRadius="4" Padding="6,2" VerticalAlignment="Center">
                                <TextBlock Text="{x:Bind ViewModel.TotalSensorCount, Mode=OneWay}" FontSize="11" Foreground="#CCCCCC" />
                            </Border>
                        </StackPanel>

                        <!-- Filter Chips -->
                        <StackPanel Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal" Spacing="6">
                            <Button Content="All" Click="Filter_Click" Tag="All" />
                            <Button Content="Temperatures" Click="Filter_Click" Tag="Temperatures" />
                            <Button Content="Loads" Click="Filter_Click" Tag="Loads" />
                            <Button Content="Clocks" Click="Filter_Click" Tag="Clocks" />
                            <Button Content="Fans" Click="Filter_Click" Tag="Fans" />
                            <Button Content="Power" Click="Filter_Click" Tag="Power" />
                        </StackPanel>
                    </Grid>

                    <!-- Sensor Items Repeater / ListView -->
                    <ListView ItemsSource="{x:Bind ViewModel.FilteredSensors, Mode=OneWay}" SelectionMode="None">
                        <ListView.HeaderTemplate>
                            <DataTemplate>
                                <Grid Padding="12,8" Background="#282828" BorderBrush="#383838" BorderThickness="0,0,0,1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2.5*" />
                                        <ColumnDefinition Width="1.5*" />
                                        <ColumnDefinition Width="1.5*" />
                                        <ColumnDefinition Width="1.2*" />
                                        <ColumnDefinition Width="1.2*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="SENSOR &amp; COMPONENT" FontSize="11" FontWeight="SemiBold" Foreground="#8A8886" />
                                    <TextBlock Grid.Column="1" Text="CURRENT READING" FontSize="11" FontWeight="SemiBold" Foreground="#8A8886" />
                                    <TextBlock Grid.Column="2" Text="CLASSIFICATION" FontSize="11" FontWeight="SemiBold" Foreground="#8A8886" />
                                    <TextBlock Grid.Column="3" Text="MIN" FontSize="11" FontWeight="SemiBold" Foreground="#8A8886" />
                                    <TextBlock Grid.Column="4" Text="MAX" FontSize="11" FontWeight="SemiBold" Foreground="#8A8886" />
                                </Grid>
                            </DataTemplate>
                        </ListView.HeaderTemplate>

                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="vm:SensorViewModel">
                                <Grid Padding="12,8" BorderBrush="#2A2A2A" BorderThickness="0,0,0,1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2.5*" />
                                        <ColumnDefinition Width="1.5*" />
                                        <ColumnDefinition Width="1.5*" />
                                        <ColumnDefinition Width="1.2*" />
                                        <ColumnDefinition Width="1.2*" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{x:Bind SensorName}" FontSize="13" FontWeight="Medium" Foreground="#FFFFFF" />
                                        <TextBlock Text="{x:Bind HardwareName}" FontSize="11" Foreground="#7A7876" />
                                    </StackPanel>

                                    <TextBlock Grid.Column="1" Text="{x:Bind FormattedValue}" FontSize="14" FontWeight="SemiBold" Foreground="#0078D4" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="2" Text="{x:Bind SensorType}" FontSize="12" Foreground="#9E9E9E" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="3" Text="{x:Bind FormattedMin}" FontSize="12" Foreground="#7A7876" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="4" Text="{x:Bind FormattedMax}" FontSize="12" Foreground="#7A7876" VerticalAlignment="Center" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
